# Workstation-side dev dependencies only. None of these run on the
# router (the router uses /opt/bin/python3 + stdlib). Install if you
# want to run diag/ utilities from your laptop, or test
# sub_to_singbox.py locally before deploying.

# HTTP client used by sub_to_singbox.py's optional IP→cc lookups when
# run on a workstation. The router-side path uses urllib only.
requests>=2.32.0,<3.0.0
