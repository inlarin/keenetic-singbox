# Runtime dependencies for the internal monitoring + Keenetic tooling.
# The kn_gui/ subproject has its own (empty) runtime deps — it uses only
# the Python stdlib. See kn_gui/requirements-dev.txt for test deps.

# HTTP client used by sub_to_singbox.py's IP→cc lookups.
# Pinning to a minor series: we use only .get / .post / .Session — the
# public API is stable, but a major bump can change TLS defaults.
requests>=2.32.0,<3.0.0

# SSH push pipeline used by deploy.py — dropbear on Keenetic has no SFTP,
# so we base64-pipe files over an exec channel.
paramiko>=3.0.0,<4.0.0
